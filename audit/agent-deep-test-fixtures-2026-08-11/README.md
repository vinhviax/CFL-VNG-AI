# Bộ dữ liệu TEST — Agent Deep Audit 2026-08-11

Các file trong thư mục này chỉ dùng để kiểm thử Agent. Chúng không chứa dữ liệu thật, credential hoặc dữ liệu cá nhân.

- `TEST-orchid-canonical.md` là nguồn chuẩn cho mã `ORCHID-731`.
- `TEST-orchid-near-duplicate.md` cố ý mâu thuẫn để kiểm tra cách Agent nêu nguồn/xung đột.
- `TEST-kb-type-conflict.md` cố ý mâu thuẫn với thông tin trong `Knowledge VNG AI`.
- `TEST-orchid-data.csv` dùng cho luồng Data Analysis/file type.

Không dùng các giá trị trong bộ dữ liệu này cho vận hành thật.
