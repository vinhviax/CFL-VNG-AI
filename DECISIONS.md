# Nhật ký quyết định — Knowledge Base VNG

Các quyết định dưới đây là quy ước bền vững. Chúng giải thích cách duy trì dự án, không thay thế hướng dẫn nghiệp vụ — nay nằm ở `docs KB/Human` (nguồn build lên Web) và `docs KB/Dev` (cơ chế và bằng chứng). Master cũ [so-tay-tao-knowledge-base-v3.md](audit/archive/so-tay-tao-knowledge-base-v3.md) đã hết vai trò, giữ trong `audit/archive/` để truy nguyên (DEC-066).

| Mã | Ngày | Trạng thái | Quyết định | Hệ quả bảo trì |
|---|---|---|---|---|
| DEC-001 | 06/08/2026 | Đang áp dụng | Master `so-tay-tao-knowledge-base-v3.md` là nguồn nội dung chuẩn duy nhất. | Sửa nội dung ở master, không sửa trực tiếp artifact sinh. |
| DEC-002 | 06/08/2026 | Được thay thế 07/08/2026 | Phân phối sổ tay thành đúng 12 module Markdown. | Được thay thế bởi DEC-009 sau khi tách hướng dẫn Google Drive thành module riêng. |
| DEC-003 | 06/08/2026 | Đang áp dụng | Cung cấp HTML offline trong một file tự chứa. | Không thêm phụ thuộc CSS, script, font hay ảnh ngoài; sinh lại bằng builder. |
| DEC-004 | 06/08/2026 | Đang áp dụng | Dùng hai đường ảnh: PNG local cho người đọc/HTML offline và URI MinIO của KB asset cho chat. | Giữ asset local, mapping và `LOCAL_ASSET` tương ứng; kiểm tra cả hai đường khi thay ảnh. |
| DEC-005 | 06/08/2026 | Đang áp dụng | Giữ KB asset và ảnh nguồn khi Markdown còn tham chiếu URI MinIO. | Không xóa `Knowledge VNG - Image Assets` hoặc ảnh độc lập trước khi có mapping thay thế và chat test đã kiểm chứng. |
| DEC-006 | 06/08/2026 | Được thay thế 07/08/2026 | Chỉ thao tác live trên hai KB test được audit ghi nhận. | Phạm vi pipeline ảnh hiện do DEC-012 quy định; KB trong audit cũ là bằng chứng lịch sử, không tự động cấp quyền mutation. |
| DEC-007 | 06/08/2026 | Đang áp dụng | Phân biệt Đã kiểm chứng, Có điều kiện và Bị chặn/Chưa xác định. | Trạng thái live luôn có ngày và liên kết bằng chứng; không suy diễn thành hướng dẫn chắc chắn. |
| DEC-008 | 06/08/2026 | Đang áp dụng | Cập nhật tài liệu bàn giao ở cuối mỗi phiên có ảnh hưởng trạng thái. | Cập nhật audit trước, rồi `STATUS.md` và `HANDOFF.md` với việc đã làm, tồn đọng, bằng chứng và bước tiếp theo. |
| DEC-009 | 07/08/2026 | Được thay thế 11/08/2026 | Phân phối sổ tay thành đúng 13 module Markdown, với `12-ket-noi-google-drive.md` là module độc lập. | Được thay thế bởi DEC-018 khi bổ sung module Agent. |
| DEC-010 | 07/08/2026 | Hoàn tất 07/08/2026 | Giai đoạn bổ sung 11 ảnh Google Drive chỉ được công nhận chat-ready khi đủ mapping và strict build đạt. | Giai đoạn thiếu mapping đã kết thúc; snapshot cũ được giữ trong audit, còn artifact bàn giao không dùng `--allow-missing-minio`. |
| DEC-011 | 07/08/2026 | Được thay thế 07/08/2026 | Từng cho phép `Knowledge VNG AI` nạp/giữ cả artifact sổ tay và ảnh 15–25 để hoàn tất mapping ban đầu. | Được DEC-012 thay thế: consumer không còn là asset host; bản module cũ chỉ được gỡ sau khi bản mới đã kiểm chứng. |
| DEC-012 | 07/08/2026 | Được mở rộng 11/08/2026 | Tách hai vai trò: `Knowledge VNG - Image Assets` (`6da8657c-dd96-4170-a698-074043475014`) giữ 25 PNG; `Knowledge VNG AI` (`cefadf09-4187-46ac-a765-591e3255a4a4`) chỉ giữ 13 MD. | Vai trò vẫn giữ nguyên; số lượng hiện hành do DEC-019 quy định. |
| DEC-013 | 07/08/2026 | Được thay thế 11/08/2026 | Hợp đồng ảnh gồm 25 PNG local, 25 URI MinIO duy nhất trong `image-map.json`, 36 link ảnh hoạt động và 36 `LOCAL_ASSET` trong 13 module; HTML dùng ảnh local để chạy offline. | Được thay thế bởi DEC-020 sau khi thêm 9 ảnh Agent và module thứ 14. |
| DEC-014 | 07/08/2026 | Đang áp dụng | File `.png` phải có payload PNG thật với magic bytes `89 50 4E 47 0D 0A 1A 0A`; không đổi đuôi JPEG để giả định dạng. | Transcode rồi kiểm tra signature trước upload/build; giữ [backup JPEG gắn sai đuôi](audit/image-assets-original-jpeg-mislabeled-2026-08-07/) và [snapshot map trước migration](audit/image-map-before-assets-migration-2026-08-07.json) làm checkpoint phục hồi. |
| DEC-015 | 07/08/2026 | Đang áp dụng | Thay ảnh theo thứ tự asset host → map → strict build → replace MD → chat test → cleanup consumer PNG. | Không đảo thứ tự, không cleanup consumer trước chat test, và không xóa PNG ở asset host khi bất kỳ Markdown nào còn tham chiếu. |
| DEC-016 | 07/08/2026 | Được thay thế 11/08/2026 | Gate bàn giao local là strict build tạo 13 module và HTML offline, sau đó chạy toàn bộ test; baseline hiện tại là 11/11. | Được thay thế bởi DEC-021 cho cấu trúc 14 module. |
| DEC-017 | 07/08/2026 | Đang áp dụng | `Test Doc RAG+Wiki` đã bị người dùng xóa và chỉ còn là bằng chứng lịch sử trong audit 06/08/2026. | Không dùng KB/ID đó như dependency hay đích mutation; kiểm thử Documents/Wiki/Graph mới cần KB test mới được người dùng cho phép. |
| DEC-018 | 11/08/2026 | Được thay thế 12/08/2026 | Phân phối sổ tay thành đúng 14 module Markdown; `13-tao-va-van-hanh-agent.md` là module độc lập sau Google Drive. | Được thay thế bởi DEC-023 khi Agent được tách thành các module 13–19. |
| DEC-019 | 11/08/2026 | Được thay thế 12/08/2026 | Giữ nguyên kiến trúc hai KB nhưng mở rộng inventory: asset host giữ 34 PNG, consumer chỉ giữ 14 MD. Agent nghiệp vụ dùng consumer làm KB trả lời, không dùng asset host làm corpus thông thường. | Được thay thế bởi DEC-024 cho inventory 49 PNG/20 MD. |
| DEC-020 | 11/08/2026 | Được thay thế 12/08/2026 | Hợp đồng ảnh hiện hành gồm 34 PNG local, 34 URI MinIO duy nhất, 45 link ảnh hoạt động và 45 `LOCAL_ASSET` trong 14 module; HTML dùng ảnh local để chạy offline. | Được thay thế bởi DEC-025 cho ảnh 35–49 và 60 cặp ảnh. |
| DEC-021 | 11/08/2026 | Được thay thế 12/08/2026 | Gate bàn giao local là strict build tạo 14 module và HTML offline, sau đó chạy toàn bộ test; baseline hiện tại là 11/11. | Được thay thế bởi DEC-026 cho 20 module/13 test. |
| DEC-022 | 11/08/2026 | Đang áp dụng | Với Agent, cấu hình Intent/prompt/model/switch trong UI và hành vi runtime là hai lớp bằng chứng riêng. | Chỉ kết luận Intent, retrieval, VLM hoặc ASR hoạt động khi chat test chứng minh; lỗi quota, attachment, raw chunk hoặc thiếu model phải ghi là Có điều kiện/Bị chặn, không suy rộng thành mặc định. |
| DEC-023 | 12/08/2026 | Đang áp dụng | Agent được phân phối thành bảy module chuyên sâu `13`–`19`; module 13 là tổng quan ngắn, module 14–19 tách cấu hình, model/quota, KB/tool/retrieval, đa phương thức, chat/evidence và vòng đời/quyền. | Giữ đúng 20 marker `MODULE`; không sửa module sinh trực tiếp. Bản 13 cũ chỉ được dọn sau source/ảnh của bản thay thế pass. |
| DEC-024 | 12/08/2026 | Đang áp dụng | Giữ kiến trúc hai KB với inventory phát hành 49 PNG asset host và 20 Markdown consumer. | Asset host vẫn không nhận Markdown; consumer vẫn không nhận PNG độc lập; Agent dùng consumer làm corpus. |
| DEC-025 | 12/08/2026 | Đang áp dụng | Hợp đồng ảnh hiện hành gồm 49 PNG local, 49 URI MinIO duy nhất, 60 link ảnh hoạt động và 60 `LOCAL_ASSET` trong 20 module. | Metadata map giữ provenance 01–25, 26–34 và 35–49; thay đổi đi qua asset host → map → strict build → Markdown → source/ảnh chat test. |
| DEC-026 | 12/08/2026 | Được thay thế 14/08/2026 | Gate local v3.3.0 là strict build tạo 20 module và HTML offline, sau đó chạy toàn bộ 13 test. | Được thay thế bởi DEC-032 sau khi bổ sung test hồi quy cho layout mới và đường dẫn có khoảng trắng. |
| DEC-027 | 12/08/2026 | Đang áp dụng | Các KB/Agent disposable Agent deep chỉ tồn tại để kiểm thử và phải được audit trước cleanup. | Chỉ xóa sau khi rollout chứng minh source/ảnh và backup legacy; giữ Agent mặc định cùng hai Agent kiểm thử có sẵn. |
| DEC-028 | 14/08/2026 | Được thay thế 14/08/2026 | Dùng `J:\My Drive\CFL\KB VNG` làm root làm việc bên ngoài workspace cho các thư mục KB và dữ liệu CFL. | Được thay thế bởi DEC-030 sau khi người dùng gom toàn bộ project vào `J:\My Drive\CFL\VNG AI\Knowledge Base VNG`. |
| DEC-029 | 14/08/2026 | Đang áp dụng | Cô lập dữ liệu định danh/hành vi tại `Data Private Weapon`; không tự động trộn vào hai KB tài liệu hiện hành. | Muốn upload hoặc dùng cho Agent/RAG phải có chỉ định KB private đích, quyền, retention, tối thiểu hóa/ẩn danh và test parser. Audit/handoff chỉ ghi schema, count và checksum, không ghi giá trị cá nhân cụ thể. |
| DEC-030 | 14/08/2026 | Đang áp dụng | Root duy nhất của project là `J:\My Drive\CFL\VNG AI\Knowledge Base VNG`; `knowledge/` quản lý dữ liệu/mirror KB trên Web và `agent/` quản lý Agent sẽ tạo. | Không duy trì thêm workspace hoặc mirror ngoài root này. Tài liệu, builder và handoff phải dùng cùng một cây thư mục. |
| DEC-031 | 14/08/2026 | Đang áp dụng | Giữ chính xác tên công ty/Web `GS9 Knowledge VNG AI` và `GS9 Knowledge VNG - Image Assets`; thay layout gộp `knowledge-vng/` bằng hai folder tương ứng dưới `knowledge/`. | Builder ghi module/map vào consumer và đọc PNG từ asset host; không đổi tên bỏ prefix `GS9`, không tái tạo `knowledge-vng/`. |
| DEC-032 | 14/08/2026 | Đang áp dụng | Gate local gồm strict build 20 module/HTML và toàn bộ 16 test. | Test khóa tên KB Web, layout hai folder, đường dẫn ảnh có khoảng trắng, master không còn SOP theo layout/count cũ, 49 PNG/URI và 60 cặp ảnh; bản bàn giao không dùng `--allow-missing-minio`. |
| DEC-033 | 14/08/2026 | Đang áp dụng | Master bị thất lạc trong lần di chuyển được phục hồi từ 20 module phát hành bằng biến đổi xác định và kiểm tra round-trip trước khi ghi. | Backup nguyên byte nằm tại `audit/workspace-normalization-prebuild-2026-08-14.zip`; 20/20 module sau build khớp nội dung cũ, chỉ thay đường dẫn `LOCAL_ASSET` và hai link audit theo layout mới. |
| DEC-034 | 14/08/2026 | Đang áp dụng | Cấu hình 10 custom Agent theo least privilege: basic tool theo vai trò được phép dùng khi no-KB; RAG/Wiki/Data/SQL/Catalog chỉ bật sau audit nguồn và quyền. Dùng meta-KB riêng `GS9 CFL Knowledge Agent` (`1d92448f-7ee2-46c4-b202-5efbe9cc5616`) cho Human hiểu/chọn/dùng 6 Agent mặc định và 10 Agent custom. | Giữ meta-KB tách khỏi consumer sổ tay và asset host; không bind meta-KB hay KB CFL nghiệp vụ vào Agent chỉ để mở khóa tool. Mọi gửi/publish/live mutation/account action vẫn cần Human approval; Web actual và runtime evidence phải được ghi riêng. |
| DEC-035 | 14/08/2026 | Đang áp dụng | Dùng repository cá nhân/private `https://github.com/vinhviax/CFL-VNG-AI`, branch `main`, làm điểm bàn giao chuyển máy cho toàn bộ snapshot project. Credential/private key/`.env` không bao giờ thuộc phạm vi Git dù người dùng yêu cầu publish toàn bộ dữ liệu còn lại; shortcut `.gsheet` không portable cũng không phải artifact Git. | Giữ `.gitignore` bảo vệ vùng key đã nhận diện, không `git add -f` hoặc force-push; dùng CSV/JSON export thật thay shortcut Google Drive; ghi audit và handoff có ngày trước khi chuyển máy. Repository này không cấp quyền tự động publish/share/mutation trên VNG AI. |
| DEC-036 | 15/08/2026 | Đang áp dụng | Quy hoạch KB theo bản đồ 7 tầng (`docs/superpowers/specs/2026-08-15-kb-architecture-design.md`) phải đi **trước** việc bind KB cho Agent, thay vì bind theo KB sẵn có. Ranh giới KB vẽ theo (mức nhạy cảm × đối tượng đọc × nhịp cập nhật), không theo nguồn dữ liệu. | `agent/kb-allowlist-proposal-2026-08-14.md` được xem là bản nháp Phase 1 cũ; ma trận Agent × KB × tool chuẩn nay lấy từ bản đồ 7 tầng. Không bind KB mới cho Agent nào nếu chưa đối chiếu bản đồ này. |
| DEC-037 | 15/08/2026 | Đang áp dụng | **Không đổi tên KB đang chạy trên Web.** Thay vào đó mỗi KB có một dòng "bản chất" ghi rõ vai trò thật trong luồng (mục 5.1 của bản đồ kiến trúc). Tên mới chỉ áp cho KB tạo mới. | Tránh phá vỡ tham chiếu/thói quen/tài liệu cũ. Khi cần biết một KB đóng vai gì, tra bảng bản chất chứ không suy từ tên. |
| DEC-038 | 15/08/2026 | Đang áp dụng | `GS9 CFL Kho Dữ Liệu Tổng Hợp` là **KB dẫn xuất có chủ đích** — gom nhiều KB lẻ để xem Wiki/Graph ở góc nhìn toàn cảnh mà KB lẻ không cho được, không phải trùng lặp nhầm. `GS9 CFL Data Daily` giữ dùng bình thường, không hạ cấp. `GS9 CFL PUM` hạ từ P1 xuống P2 nội bộ vì cả team được đọc doanh thu, nhưng cấm số liệu doanh thu lọt vào nội dung gửi người chơi vẫn giữ nguyên. | Bật nhóm tool Wiki cho Agent bind KB tổng hợp (ngoại lệ có cơ sở với quy tắc giữ Wiki Off). Không Agent nào được bind đồng thời KB tổng hợp và KB lẻ cấu thành nó. Nhịp sự kiện LiveOps xác nhận là theo tháng. |
| DEC-039 | 15/08/2026 | Đang áp dụng | Đổi mục đích Agent `GS9 GM Case Investigator` → `GS9 CFL GM Policy Advisor`: từ điều tra case-scoped (không khả thi trên nền tảng, vi phạm least privilege) sang tra cứu chính sách/quy trình/tiền lệ, với bằng chứng vụ việc qua tệp đính kèm hội thoại thay vì KB. Áp cả tên, mô tả và System Prompt lên Web ngày 15/08/2026. | KB đích đổi từ "case-scoped view" (không tồn tại) sang `GS9 CFL GM Policy & Sanction` (L6, cần tạo). Bỏ `Truy vấn CSDL` khỏi lộ trình tool. 4 file `agent/GS9 CFL GM Policy Advisor/` và hồ sơ meta-KB đã cập nhật theo vai trò mới. |
| DEC-040 | 15/08/2026 | Đang áp dụng | Hai guardrail bắt buộc khi bind `GS9 CFL Plan Version` cho Agent: **G-A** (chống trộn phiên bản — Agent phải nêu rõ fact thuộc phiên bản nào, hỏi lại nếu người dùng không nêu phiên bản) áp cho mọi Agent bind KB này; **G-B** (kế hoạch nội bộ không phải brief đã duyệt — cấm trích nguyên văn vào nội dung gửi người chơi, cấm trình bày mục chưa chốt như đã lên sóng) áp riêng cho `GS9 CFL Player Communications`. Nguyên văn hai câu tại mục 12.1 của bản đồ kiến trúc. | Bắt buộc vì Plan Version dùng chung một KB cho mọi phiên bản game (V5, V6, …) theo quyết định người dùng — không có guardrail thì Agent sẽ trộn phiên bản hoặc coi kế hoạch chưa chốt là đã phát hành. Áp guardrail là mutation live, thực hiện cùng lúc với thao tác bind. |
| DEC-041 | 15/08/2026 | Đang áp dụng | Cú pháp nhúng ảnh chuẩn `<!-- LOCAL_ASSET: <path> -->` + `![mô tả](minio://.../exports/<uuid>.<ext>)` được xác nhận **hoàn toàn đúng**; hiện tượng ảnh render hai lần trong chat là hành vi không ổn định của model khi soạn câu trả lời, không phải lỗi cú pháp hay cấu trúc KB. | Làm rõ DEC-004. Không sửa `build_handbook.py`/`convert_cfl_plan_html.py`/`link_plan_v5_minio.py` vì lý do này, không điều tra lại. Bằng chứng: `audit/cfl-plan-v5-chat-image-double-render-2026-08-15.md`. |
| DEC-042 | 15/08/2026 | Đang áp dụng | Quy ước đặt tên: ảnh dùng tiền tố `image-`, tài liệu không phải ảnh dùng tiền tố `doc-`. Tài liệu đồng bộ từ Google Drive là ngoại lệ (người dùng tự đặt tên). KB nhiều phiên bản chèn số phiên bản ngay sau tiền tố: `doc-v5-NN-...`, `image-v5-NN-...`. | Áp dụng cho toàn bộ tài liệu mới và đã đổi tên: 20 module sổ tay, 49 PNG, 25 file meta-KB Agent, 12 Markdown + 29 JPEG Plan V5. Chi tiết tra cứu: `knowledge/GS9 CFL Knowledge Agent/doc-32-quy-uoc-dat-ten-va-nhung-anh.md`. |
| DEC-043 | 15/08/2026 | Đang áp dụng | Gộp vật lý 49 PNG từ `GS9 Knowledge VNG - Image Assets` vào cùng thư mục `GS9 Knowledge VNG AI` (đổi tên `image-NN-...`); bỏ khái niệm `ASSET_KB_NAME`/asset-dir riêng trong `build_handbook.py`, `local_asset_prefix` đổi thành `.` (cùng thư mục). | Thay thế DEC-012, DEC-019, DEC-024. Web KB `GS9 Knowledge VNG - Image Assets` (`6da8657c-...`) **chưa bị xoá hay đổi tên trên Web** — chỉ local đã gộp; xoá KB đó trên Web phải chờ Phase 3 (Drive sync) hoàn tất và chat-test đạt (giữ nguyên tinh thần DEC-005). `image-map.json` ghi rõ URI hiện tại vẫn do KB cũ cấp cho tới khi resync. |
| DEC-044 | 15/08/2026 | Đang áp dụng | `GS9 Knowledge VNG AI` = tri thức nền tảng dùng chung cho cả team GS9, gồm tài liệu hai tính năng Knowledge Base (`doc-00`→`doc-12`) và Agent (`doc-13`→`doc-19`) kèm 49 ảnh của chính hai tính năng đó (69 tài liệu). `GS9 CFL Knowledge Agent` = 6 Agent mặc định + 10 Agent custom CFL + cấu trúc/KB riêng của CFL (28 tài liệu, gồm 3 trang mới `doc-30`–`doc-32` dịch bản đồ 7 tầng sang dạng tra cứu cho cả team). | Bổ sung DEC-034. Nội dung nghiệp vụ của 20 module không đổi, chỉ đổi tên và gộp thư mục ảnh. Ba trang `doc-30`–`doc-32` là nội dung mới, không thuộc ràng buộc bảo toàn nội dung của việc đổi tên. |
| DEC-045 | 15/08/2026 | Đang áp dụng | Ưu tiên MCP `list_documents` để thu URI MinIO hàng loạt (trả `file_path` cho toàn bộ tài liệu trong một lệnh) thay vì bắt network bằng browser. Điều kiện: KB phải được share vào space trước. | Rút chi phí thu URI từ hàng chục/trăm lượt thao tác browser xuống 1–2 lệnh MCP. Agent không tự share KB vào space — đó là thay đổi quyền, cần người dùng thực hiện hoặc cho phép rõ. Còn phải kiểm chứng dạng `file_path` cho tài liệu ảnh (khác dạng `.../exports/<uuid>.<ext>` mà `image-map.json` dùng) trước khi dùng làm nguồn ghi map. |
| DEC-046 | 15/08/2026 | Đang áp dụng | Đưa nội dung lên KB Web bằng Google Drive connector thay vì upload tay; mỗi KB trỏ đúng **một** thư mục con của `knowledge/` (`GS9 Knowledge VNG AI` → `knowledge/GS9 Knowledge VNG AI/`, `GS9 CFL Knowledge Agent` → `knowledge/GS9 CFL Knowledge Agent/`, `GS9 CFL Plan Version` → `knowledge/GS9 CFL Plan Version/`); tuyệt đối không trỏ root project. `GS9 CFL Item Profile` không sync. | Root project chứa `audit/`, `docs/`, `scripts/`, `tests/`, `.git/` và service-account key trong `knowledge/GS9 CFL Item Profile/keys CFL ItemID/keys/` — không được lọt vào phạm vi sync. Hành vi đổi tên khi sync (135 file đổi tên trong phiên này) **chưa được kiểm chứng trên nền tảng**; bắt buộc thử trên 2–3 file trước khi sync toàn bộ. Phase 3 (sync thật) **chưa thực hiện** trong phiên 15/08 tạo ra các DEC này — cần go-ahead riêng. |
| DEC-047 | 15/08/2026 | Đang áp dụng | Mở rộng quy ước đặt tên `doc-` (DEC-042) sang 5 KB ngoài kế hoạch gốc, theo yêu cầu trực tiếp của người dùng: `GS9 CFL PUM` (7 PDF), `GS9 CFL Data Daily` (8 XLSX), `GS9 CFL Sentiment Feedback User` (1 XLSX), `GS9 CFL Glossary & Systems` (3 MD, KB nháp), `GS9 CFL CS FAQ & Policy` (1 MD, KB nháp). Giữ nguyên phần tên gốc, chỉ thêm tiền tố; `desktop.ini` không đổi. | Người dùng **xác nhận rõ và chủ động chấp nhận** các thư mục này đang liên kết Google Drive nên đổi tên local sẽ khiến Web tự đồng bộ đổi tên theo — khác với cách "thử nhỏ trước" áp cho Phase 3 của 3 KB nền tảng chính. `GS9 CFL Item Profile` loại trừ hoàn toàn (boundary P0, không đổi tên/không mở/không đọc). `GS9 CFL Kho Dữ Liệu Tổng Hợp` không có mirror local nên không áp dụng. |
| DEC-048 | 15/08/2026 | Đang áp dụng | Đổi tên local 10 custom Agent từ tiền tố `GS9 ...` sang `GS9 CFL ...`, khớp quy ước đặt tên KB (`GS9 CFL Knowledge Agent`, `GS9 CFL PUM`, v.v.). Áp dụng cho: folder `agent/GS9 CFL <Agent>/`, hồ sơ meta-KB `doc-20`→`doc-29`/`doc-27`, mọi tài liệu governance sống (`STATUS.md`, `HANDOFF.md`, `PROJECT.md`, `NEXT_SESSION_PROMPT.md`, bản đồ kiến trúc). Giữ nguyên `GS9 GM Policy Advisor` (không hoàn tác DEC-039). | Theo yêu cầu người dùng: **chỉ đổi tên local**, người dùng tự đổi tên thật trên Web. Không đổi các chỗ chỉ nhắc tên Agent dạng rút gọn không có tiền tố `GS9` (ví dụ bảng so sánh `doc-02`, sơ đồ `doc-03`) vì không mang tiền tố cần đổi. Không đổi `audit/*.md` và `agent/kb-allowlist-proposal-2026-08-14.md` (bằng chứng lịch sử, đã có vai trò "giữ nguyên" từ trước). Link chết cũ trỏ `27-custom-gs9-gm-case-investigator.md` (đã ghi ở STATUS/HANDOFF) không nằm trong phạm vi này. |
| DEC-049 | 15/08/2026 | Đang áp dụng | Mutation live trên Web do người dùng thực hiện cho **cả 10 custom Agent**: (a) đổi tên sang tiền tố `GS9 CFL`; (b) bật `Mô hình xếp hạng lại` = `bge-reranker-v2-m3`; (c) bật `Tải ảnh` + `Mô hình VLM` = `qwen3.6-plus` (audio vẫn Off); (d) chia sẻ vào space `CFL Member` quyền **Được chỉnh sửa**. Agent xác minh read-only ngày 15/08/2026 và ghi vào 10 file `agent/GS9 CFL .../config.md`. | Nâng identity/config UI-visible của 10 Agent lên **Đã kiểm chứng** cho ngày 15/08. Ba hệ quả phải theo dõi: (1) quyền `Được chỉnh sửa` cho space 6 người nghĩa là **người khác sửa được Agent** — mọi snapshot config có thể lệch bất cứ lúc nào, phải audit lại trước khi kết luận; (2) bật tải ảnh mở thêm bề mặt đưa ảnh chứa PII người chơi vào hội thoại, cần nhắc trong hướng dẫn dùng; (3) reranker và VLM đều **chưa được chat-test** nên chất lượng vẫn là *Bị chặn–Chưa xác định*. |
| DEC-050 | 15/08/2026 | Đang áp dụng | Phát hiện ngoài khai báo khi xác minh: **`Chế độ suy nghĩ` (Thinking) đang BẬT** trên Agent đã kiểm (`GS9 CFL CS Copilot`, `GS9 CFL Knowledge Curator`) — trái với baseline `Thinking Off` ghi từ 14/08. Ghi nhận trạng thái thật, không tự tắt. | Thinking bật làm tăng độ trễ và token/lượt; cần cân nhắc khi chạy G6 và khi tính quota. **Chưa xác minh trên đủ 10 Agent** — mới 2/10 đọc trực tiếp, phần còn lại suy ra theo mẫu và phải ghi rõ là *suy ra* cho tới khi kiểm đủ. Không đảo trạng thái này nếu chưa có chỉ định của người dùng. |
| DEC-051 | 16/08/2026 | Đang áp dụng | **DEC-043 (gộp ảnh + tài liệu chung một KB) được xác nhận đúng bằng thực nghiệm.** KB test `Test` (`d7295a59-03b1-496c-86eb-9ecaa3364aa7`, 2 ảnh + 2 doc tham chiếu ảnh qua URI MinIO) trải qua 3 vòng sync trên Google Drive connector: (1) Toàn bộ lần đầu — ingest sạch; (2) Tăng dần, sửa nội dung `b.md` không đụng dòng ảnh D — ảnh vẫn còn, chỉ `b.md` được re-ingest; (3) Toàn bộ, thêm file mới `e.md` + sửa nội dung cả `a.md` (ảnh C) và `b.md` (ảnh D) không đụng dòng ảnh — ảnh C và D vẫn còn nguyên ở cả hai chế độ. Kết luận: gộp ảnh + tài liệu chung KB an toàn cho việc sửa nội dung tài liệu khác miễn không đụng dòng ảnh, ở cả hai chế độ đồng bộ. | Xem chi tiết `audit/google-drive-incremental-sync-test-2026-08-15-baseline.md`. **Lý do kỹ thuật suy ra** (không xác minh bằng log connector): link ảnh nhúng là URI MinIO tĩnh, không phải tham chiếu động vào document ảnh trong KB — nên ảnh sống/chết không phụ thuộc việc tài liệu ảnh gốc (`C.png`/`D.png`) có được re-sync hay không. **Giới hạn xác minh:** không so sánh được `knowledge_id` qua MCP (`list_knowledge_bases`/`get_document_info` không truy cập được KB `Test` — có thể do phạm vi KB đã đăng ký cho MCP server bị giới hạn ở các KB production, không gồm KB test); kết luận dựa trên quan sát UI (nội dung xem trước, ảnh render, danh sách tài liệu, timestamp tải lên), không phải log hệ thống. Chưa test case sửa trực tiếp nội dung/caption của chính `C.png`/`D.png` — ngoài phạm vi câu hỏi gốc. Không cần hoàn tác kiến trúc gộp của `GS9 Knowledge VNG AI`; Phase 3 (sync thật cho KB production) có thể tiếp tục theo kế hoạch đã gộp. |
| DEC-052 | 16/08/2026 | Đang áp dụng | **URI ảnh dạng `file_path`** (`minio://knowledge-base-prd/10012/<knowledge_id>/<uuid>.<ext>`, lấy qua MCP `list_documents`) **render đúng trong Document preview**, tương đương dạng `exports/` cũ đã biết. Kiểm bằng cách gắn thử vào `doc-02-tao-kb-nhanh-va-nang-cao.md` (KB `GS9 Knowledge VNG AI`), sync Tăng dần, mở lại tài liệu — ảnh hiện đúng. Sau đó: viết lại `image-map.json` bằng `file_path` cho toàn bộ 49 ảnh (49/49 tên khớp giữa local và Web, `parse_status: completed` 100%), build lại 20 module — cả 60 lượt tham chiếu ảnh nay dùng URI mới còn sống (URI cũ trong `image-map.json` sinh 11/08 đã chết toàn bộ 49/49 — nguyên nhân là 49 ảnh được **di trú sang KB khác** theo DEC-043, không phải một lượt đồng bộ Drive thường; **xem đính chính DEC-069**, không phải mọi lần đồng bộ đều làm chết URI). | Giải quyết điểm treo của DEC-045 (dạng `file_path` cho tài liệu ảnh "chưa ai kiểm chứng"). Test `test_live_project_has_exact_agent_deep_split_and_sixty_image_pairs` nới từ khoá cứng `exports/` sang đếm theo tiền tố chung `10012/`. **Cập nhật 16/08/2026 (chat-test thật, gate đóng):** hỏi KB `GS9 Knowledge VNG AI` câu buộc trả lời kèm ảnh — **ảnh render đúng trong câu trả lời chat**, nội dung khớp `doc-02`, dẫn `Nguồn tham khảo (9 tài liệu)`. Dạng `file_path` do đó **Đã kiểm chứng** ở cả Document preview lẫn chat. **Khiếm khuyết hiển thị đi kèm:** ảnh bị render **hai lần** (đo được 2 thẻ `<img>` cùng trỏ một blob URL) và lộ ký tự markdown thừa `![`, `](`, `)` quanh ảnh — tái hiện đúng hiện tượng đã ghi ở `audit/cfl-plan-v5-chat-image-double-render-2026-08-15.md`. Suy ra (chưa có log xác nhận): nền tảng tự render khi gặp chuỗi `minio://` mà không đợi cú pháp markdown khép kín. Đây là lỗi phía nền tảng, không phải lỗi URI hay tài liệu — không cần sửa nội dung. |
| DEC-053 | 16/08/2026 | Đang áp dụng | **Tách nội dung theo đối tượng đọc.** Tạo `docs KB/Dev` (cơ chế, kết quả kiểm chứng, cấu hình — cho Dev và Agent config, **không** lên Web) và `docs KB/Human` (hướng dẫn thao tác — nguồn build ra `knowledge/`, lên Web). Đặt tên phẳng theo tính năng: `KB-*`, `Agent-*`; thêm tính năng mới thì thêm tiền tố, không tạo thư mục con. Quy tắc phân loại: câu hỏi người dùng cuối đặt khi đang chat → Human; câu hỏi chỉ người sửa hệ thống mới cần → Dev. Hai file `_QUY-UOC-VIET-FILE-DEV.md` và `_QUY-UOC-VIET-FILE-HUMAN.md` giữ chuẩn biên tập. Đã viết 20 file Dev (~5.100 dòng) và 20 file Human (~2.355 dòng). | Thay thế cách tổ chức cũ: master `so-tay-tao-knowledge-base-v3.md` vốn trộn hướng dẫn với nhật ký kiểm chứng, khiến tài liệu lên Web chứa mã DEC, link `audit/`, mục "Chưa được phép suy luận" — người dùng cuối không cần và bị rối. Master nay chỉ còn là nguồn dự phòng; `build_handbook.py` đọc `docs KB/Human` trước, chỉ fallback về master khi thư mục đó rỗng. Khi build, tiền tố tính năng đổi thành `doc-` để giữ DEC-042 (regex sync khoá `^(doc\|image)-`). Test `test_agent_modules_are_operational_guides_without_dev_content` thay gate cấu trúc heading cố định cũ (vốn bắt buộc có `Bằng chứng`, `Data flow` — chính là nội dung phải cắt) bằng gate kiểm rò rỉ nội dung Dev. **Việc còn dở:** `knowledge/GS9 CFL Knowledge Agent` (28 file viết tay) chưa tách — 28/28 file còn `Mức bằng chứng:`, 19/28 còn `SRC-CUSTOM`/`Bị chặn`. |
| DEC-054 | 16/08/2026 | Đang áp dụng | **Ảnh bundle Plan V5 nằm thẳng trong `knowledge/GS9 CFL Plan Version/V5/`**, bỏ thư mục con `assets/`. Sửa đồng bộ ba nơi: `convert_cfl_plan_html.py` (`assets_dir = output_dir`, `relative_path = filename`), `link_plan_v5_minio.py` (`ASSETS = BUNDLE`, regex nhận cả dạng có và không có tiền tố `assets/`, template `<!-- LOCAL_ASSET: {name} -->`), và 29 dòng `LOCAL_ASSET` trong 6 file `.md`. | Theo yêu cầu người dùng. Trước khi sửa, ba thành phần lệch pha nhau: ảnh thật và linker đã phẳng nhưng converter vẫn ghi vào `assets/` — chạy lại converter sẽ sinh **hai bộ 29 ảnh trùng tên ở hai chỗ** vì bước dọn không thấy ảnh phẳng để xoá. Regex linker cố tình vẫn nhận tiền tố `assets/` cũ để bundle sinh trước 16/08 chạy lại được. `link_plan_v5_minio.py --check` trả 0 thay đổi (idempotent), `Ran 29 tests OK`. **Chưa kiểm chứng end-to-end:** không chạy lại được converter vì HTML nguồn chỉ có trên máy công ty, nên 6 test converter vẫn skip. |
| DEC-055 | 16/08/2026 | Đang áp dụng | **`GS9 CFL Item Profile` đổi 7 file `.csv` sang đúng quy ước `doc-NN-slug`** (từ `doc_01_weapons_usage.csv` → `doc-01-weapons-usage.csv`). Giữ nguyên `doc_CFL ItemID.gsheet`. Đảo một phần DEC-047 (vốn ghi KB này "loại trừ hoàn toàn — không đổi tên"). | Theo chỉ định trực tiếp của người dùng ngày 16/08/2026. Lý do: tên cũ dùng **gạch dưới** nên regex lọc `^(doc\|image)-` của Google Drive connector **không** khớp — KB này sẽ không đồng bộ được nếu sau này cần. Thao tác **chỉ đổi tên, không mở và không đọc nội dung**; boundary P0 với dữ liệu định danh người chơi vẫn giữ. `.gsheet` không đổi vì là shortcut Drive, không phải artifact nạp lên KB (DEC-035). Việc KB này có nên sync lên Web hay không **vẫn chưa quyết** — đổi tên chỉ gỡ rào kỹ thuật, không phải phê duyệt đưa dữ liệu người chơi lên KB dùng chung. |
| DEC-056 | 16/08/2026 | Đang áp dụng | **Chèn luật trích dẫn ảnh vào `System Prompt` của cả 10 Agent custom `GS9 CFL *`** (mutation live trên Web, do agent thực hiện theo chỉ định trực tiếp của người dùng). Đoạn luật buộc Agent chép nguyên dòng `![alt](minio://...)` từ nguồn truy hồi vào câu trả lời, cấm bịa/rút gọn/dựng lại URI, đặt ảnh cạnh bước mà nó minh hoạ, và không than phiền khi nguồn không có ảnh. Chèn ngay trước `Reply in {{language}}.` — cả 10 prompt gốc đều có câu này. Nội dung và lý do từng câu ở `docs KB/Dev/Agent-prompt-trich-dan-hinh-anh.md`. | Xác minh bằng cách mở lại từng Agent và **đọc ngược** `System Prompt` sau khi lưu: 10/10 chứa chuỗi `minio://`, độ dài 1441–1702 ký tự. **`GS9 CFL CS Copilot` là Agent duy nhất không gắn KB nào** nên luật đã dán nhưng chưa có tác dụng — không có nguồn để lấy URI; sẽ tự hiệu lực khi được bind KB. **Chưa chat-test** luật này trên bất kỳ Agent nào — chưa biết mô hình có tuân thủ hay không, và chưa biết hiện tượng double-render (DEC-052) có xuất hiện trong chat Agent như ở chat KB hay không. Agent nên test là `GS9 CFL Knowledge Curator` vì đã xác minh gắn `GS9 Knowledge VNG AI` (KB có ảnh). Rủi ro cần theo dõi: space `CFL Member` có quyền `Được chỉnh sửa` (DEC-049) nên người khác có thể ghi đè prompt này bất cứ lúc nào. |
| DEC-057 | 16/08/2026 | Đang áp dụng | **Vá 29 URI ảnh của bundle Plan V5** sau khi người dùng nạp lại ảnh lên KB `GS9 CFL Plan Version`. URI cũ dạng `exports/<uuid>` (map sinh 15/08) đã chết toàn bộ. Lấy URI mới qua API nội bộ `miniapp.vnggames.ai/kb/v1/api/knowledge-bases/<id>/knowledge` (trường `file_path`), viết lại `image-map.json`, rồi thay 58 tham chiếu trong 7 file `.md` (29 ảnh nhúng + 29 link registry). Nới `URI_PREFIX` trong `link_plan_v5_minio.py` từ `.../10012/exports/` xuống `.../10012/` để nhận cả hai dạng hợp lệ (DEC-052). | **Cách lấy URI khi MCP không truy cập được KB:** `list_knowledge_bases` của MCP chỉ thấy 6 KB đã share vào space, không có `GS9 CFL Plan Version`. Đường vòng đã dùng: lấy token qua `/api/auth/token?sub_app=kb` rồi gọi API miniapp kèm `Authorization: Bearer` — gọi thẳng bằng cookie bị CORS chặn. Ghi lại vì đây là cách duy nhất hiện biết để thu URI hàng loạt cho KB chưa share. **Cách xác định ảnh nào ứng URI nào:** dựa vào comment `<!-- LOCAL_ASSET: <tên file> -->` ngay trên dòng nhúng, và tên file làm link text ở registry — không đoán theo thứ tự. Đối chiếu 29/29 tên khớp giữa local và Web, `parse_status: completed` 100%. Bổ sung test `test_bundle_uris_match_map_and_are_valid_minio` khoá hợp đồng này; `Ran 30 tests OK`. **Chưa chat-test** ảnh V5 trên Web. |
| DEC-058 | 16/08/2026 | Đang áp dụng | **Bảng "Agent nào gắn KB nào" bổ sung vào cả bản Human (`AgentCFL-02`) và Dev (`Agent-ho-so-16-agent-cfl.md`)**, lấy từ dữ liệu thật qua `GET /kb/v1/api/agents` (trường `config.kb_selection_mode`, `config.knowledge_bases`) — không phải suy từ tài liệu cũ. Kết quả: 6/10 Agent custom có KB, 4/10 chưa gắn gì. Hai binding từng bị đánh giá rủi ro trước đó — `Incident Triage → GS9 CFL PUM` (số liệu doanh thu) và `Player Voice Analyst → GS9 CFL Sentiment Feedback User` (phản hồi người chơi) — **người dùng xác nhận giữ nguyên**, lý do nền tảng chỉ dùng nội bộ. | Trước đó tài liệu hoàn toàn không có bảng này — người dùng phải tự đoán trợ lý tra nguồn nào. Hồ sơ Agent cũ và audit 14/08 từng đánh dấu hai binding trên là *"chặn"* / *"không bind as-is"*; các đánh giá đó **không còn hiệu lực** cho hai cặp này kể từ 16/08/2026, không cần cảnh báo lại trong các phiên sau. Bốn Agent chưa gắn KB (`CS Copilot`, `Economy Offer Analyst`, `GM Policy Advisor`, `Player Communications`) trả lời không có nguồn CFL nào bảo chứng — luật trích dẫn ảnh (DEC-056) đã dán nhưng chưa có tác dụng ở nhóm này. Binding có thể đổi bất cứ lúc nào (space quyền Được chỉnh sửa, DEC-049) nên bảng này là ảnh chụp 16/08/2026, không phải sự thật vĩnh viễn — phiên sau cần đọc lại API trước khi tin. |
| DEC-059 | 17/08/2026 | Đang áp dụng | **Sự cố Google Drive mất kết nối rồi tự "Restore"**, trộn layout cũ (trước 15/08) chồng lên cấu trúc hiện hành: thư mục `knowledge` thật bị Drive đẩy thành `knowledge (1)`, `.git` mất lịch sử, ~260 file tracked lệch nội dung, hàng chục file/thư mục rác chui vào (`knowledge_cu`, bản trùng `(1)`, `desktop.ini`-only). **Khôi phục hoàn toàn, không mất dữ liệu đã commit**: clone bản sạch từ GitHub (`5ff4aac`) ra ngoài Drive để đối chiếu trước khi sửa gì; đổi tên `knowledge (1)` → `knowledge` (giữ nguyên ID thư mục nên **không đứt kết nối connector**, khác với xoá-rồi-tạo-mới); thay `.git` hỏng bằng bản sạch từ clone; `git restore .` đưa 260 file tracked về đúng commit; `git clean -f knowledge/` dọn 59 file rác trong đó; rà soát thủ công toàn cây thư mục tìm file/thư mục kiểu Drive-duplicate (`(1)`, `(2)`, `Copy of`, thư mục chỉ có `desktop.ini`) — xoá 6 thư mục rác: `V5/assets`, `V5/assets (1)`, `docs KB/Dev/Agent`, `docs KB/Dev/KB`, `docs KB/Asset/New folder`, `knowledge/Test`. | Xác nhận cuối bằng `git diff --stat origin/main HEAD` rỗng tuyệt đối (không chỉ tin `git status`) và `Ran 30 tests OK`. `knowledge/Test` (KB sandbox test DEC-051/052) mất file cục bộ thật (`a.md`, `b.md`, `e.md`, `C.png`, `D.png`) — nhưng chưa từng được git theo dõi và **kết luận rút ra từ nó đã lưu vĩnh viễn** trong DEC-051/052 và `audit/google-drive-incremental-sync-test-2026-08-15-baseline.md`, nên không mất giá trị quyết định. `docs KB/Asset/GS9 CFL Plan Version/V5/` (79 file, bản backup ảnh riêng của người dùng) không bị ảnh hưởng — đã xác nhận không script nào đọc từ đó nên không có rủi ro dùng nhầm. **Bài học ghi vào memory hệ thống:** khi làm việc trên Google Drive, ghi file phải qua file tạm rồi `os.replace`, không ghi đè trực tiếp — ghi đè trực tiếp từng làm mất sạch nội dung một file trong phiên này (mục 4 của HANDOFF.md). |
| DEC-060 | 17/08/2026 | Đang áp dụng | **Gắn kho tri thức tạm cho 4 Agent chưa có nguồn** — `Player Communications` → `GS9 CFL Plan Version`, `Economy Offer Analyst` → `GS9 CFL Data Daily`, `CS Copilot` và `GM Policy Advisor` → `GS9 Knowledge VNG AI` (fallback nền tảng). Cả 4 trước đó ở `kb_selection_mode: none`. Đã đọc lại trực tiếp từ dialog Web sau khi lưu để xác minh, không chỉ tin toast. Bằng chứng: `audit/agent-kb-binding-4-agent-2026-08-17.md`. | Nguyên nhân 4 Agent này rỗng nguồn **không phải thiếu quyền mà là thiếu nội dung**: 4 KB nghiệp vụ đúng (`GS9 CFL CS FAQ & Policy` mới có file định dạng; `GS9 CFL Item Catalog`, `GS9 CFL GM Policy & Sanction`, `GS9 CFL Event Calendar & Brief` chưa tồn tại). Người dùng chọn hoãn soạn nội dung và gắn tạm kho gần đúng nhất để Agent không trả lời rỗng, thay vì để trống chờ. Đây là **giải pháp cầu, không đóng được lý do chặn ban đầu ở DEC-058**: `CS Copilot` và `GM Policy Advisor` đang trỏ kho hướng dẫn nền tảng hoàn toàn không chứa chính sách CS hay điều khoản xử phạt, nên câu trả lời của chúng về hai mảng đó vẫn không có nguồn CFL bảo chứng. Khi KB đúng có nội dung thì phải đổi binding và cập nhật lại bảng ở `AgentCFL-02-ma-tran-so-sanh-16-agent.md` (Human) và `Agent-ho-so-16-agent-cfl.md` (Dev). Chất lượng runtime của cả 4 vẫn **Bị chặn–Chưa xác định** vì chưa chat-test. |
| DEC-061 | 17/08/2026 | Đang áp dụng | **Bật công cụ truy hồi (`Tìm theo ngữ nghĩa` + `Tìm theo từ khóa`) cho 3 Agent vừa gắn kho tạm** — `Player Communications` 3→5 tool, `Economy Offer Analyst` 3→5, `GM Policy Advisor` 2→4. Giữ Wiki, SQL, `Danh mục sản phẩm`, `Phân tích dữ liệu` tắt theo quy ước cũ. `CS Copilot` ở chế độ `Trả lời nhanh` không có tab Công cụ nên không cần bật. | Phát hiện khi chat-test: gắn KB xong Agent **vẫn không đọc được kho**, cây suy luận trả về `tool not found: search_knowledge_base (available: ask_user, thinking, todo_write)` rồi Agent quay ra hỏi lại người dùng. Nguyên nhân: công cụ truy hồi là tập cấu hình riêng, trước đây bị mờ trên UI vì Agent chưa có KB trong phạm vi (hành vi đã ghi từ 14/08), nên sau khi bind mới bật được — và **không tự bật**. Nghĩa là DEC-060 một mình chưa làm 4 Agent dùng được. **Rút thành quy tắc: dựng Agent mới phải có bước kiểm 'đã bật công cụ truy hồi chưa'**, nếu không Agent nhìn thì có kho mà thực tế rỗng. Bằng chứng: `audit/agent-chat-test-2026-08-17.md`. |
| DEC-062 | 17/08/2026 | Đang áp dụng | **Chat-test thật 3 Agent trên KB an toàn, ghi kết quả nguyên trạng gồm cả lỗi.** `Player Communications` × `Plan Version` **đạt** (31 bước/5m24s, nội dung khớp nguồn, ảnh render thật, tự để trống ô nguồn ở 2/5 khẳng định thiếu bằng chứng). `Knowledge Curator` **có điều kiện** — từ chối bịa điều khoản xử phạt (đúng) nhưng khẳng định sai rằng hệ thống chỉ có 6 Agent mặc định và liệt kê một KB đã bị gỡ. `CS Copilot` **đạt về guardrail** — từ chối trả lời vì kho đang gắn không phải nguồn CS được duyệt. | Đây là lần đầu có Agent đạt chat-test theo nghĩa đầy đủ, mở một phần gate 4.2; 13/16 Agent vẫn *Bị chặn–Chưa xác định*. Ba kết quả này được đưa vào `gioi-thieu-knowledge-base-va-agent.html` mục 09 làm case study, **giữ nguyên cả phần lỗi** vì chúng chính là lập luận cho việc phải dựng đủ kho nghiệp vụ. Case 3 là bằng chứng trực tiếp rằng **kho tạm sai chuyên môn không thay được kho đúng** — bổ sung cho DEC-060. Lỗi truy hồi của `Knowledge Curator` (kho có nội dung nhưng lấy nhầm đoạn) là việc còn mở, chưa xử. Không test 3 Agent gắn KB nhạy cảm (PUM, Sentiment, Kho Tổng Hợp) vì người dùng giới hạn phạm vi ở KB an toàn. |
| DEC-063 | 17/08/2026 | Đang áp dụng | **DEC-054 (bố cục ảnh phẳng Plan V5) đã kiểm chứng end-to-end.** Chạy `scripts/convert_cfl_plan_html.py` thật trên máy công ty (có HTML nguồn), 6 test converter trước đây skip nay chạy và **pass toàn bộ** (`Ran 30 tests / OK`, 0 skip). Đối chiếu 29 ảnh sinh ra với bundle đang commit: **khớp tuyệt đối SHA256 29/29**. Sửa 1 dòng còn sai trong `tests/test_convert_cfl_plan_html.py:94` (đang tìm `output/"assets"/*.jpg`, phải là `output/*.jpg` để khớp bố cục phẳng). | Việc phiên trước ghi "chưa kiểm chứng end-to-end" (DEC-054) và mục 4.8 HANDOFF nay đóng. Không phát hiện lệch nào giữa converter và bundle thật — bố cục phẳng ổn định qua re-run (idempotent, `first == second` tree snapshot). Gate chuẩn từ nay là `Ran 30 tests/OK` không skip trên máy có HTML nguồn Plan V5; máy không có HTML nguồn vẫn skip đúng 6 test đó, không phải lỗi. |
| DEC-065 | 17/08/2026 | Đang áp dụng | **Chủ đề KB thêm mới đánh số từ 20 trở đi; thêm `KB-20-thiet-ke-danh-muc-kho.md`.** Dải `KB-NN` kín 00–12 và `Agent-NN` chiếm 13–19, mà builder cắt tiền tố rồi ghép `doc-` nên `KB-13` sẽ đụng `Agent-13`. Chọn số 20 làm slot trống đầu tiên. **Không cần sửa `HUMAN_SOURCE_PREFIXES`** — hằng đó chỉ liệt kê tiền tố (`KB`, `Agent`), không khoá dải số. Phải sửa 5 chỗ khoá cứng số 20: `scripts/build_handbook.py` (`EXPECTED_MODULE_COUNT` 20→21), `tests/test_build_handbook.py` (3 chỗ: `expected_modules` thêm `doc-20`, fixture giả thêm module thứ 21 và đổi `sub` sang `13 <= index < 20`, đếm URI MinIO 60→61), `so-tay-tao-knowledge-base-v3.md` và `PROJECT.md`. | Nội dung "nên dựng những loại kho nào" trước đó **chỉ có trong file HTML thuyết trình**, Agent không tra được. Nay đã vào kho `GS9 Knowledge VNG AI` thành `doc-20`. Builder có gate **mỗi module phải có ít nhất 1 ảnh** — KB-20 là tài liệu tư duy thiết kế, không có màn hình để chụp, nên dùng lại `image-06-chia-se-va-phan-quyen.png` vì nó minh hoạ đúng ràng buộc cốt lõi của tài liệu (quyền đặt ở cấp kho, không đặt cho từng tài liệu). **Chọn ảnh này có chủ ý**: nó là màn hình cấu hình, **không chứa danh sách kho hay agent**, nên không tạo thêm rủi ro "ảnh lỗi thời thành dữ kiện" đang điều tra ở mục 4.5. Ảnh dùng lại làm tổng tham chiếu ảnh tăng 60→61, không phải thêm ảnh mới vào kho. Gate sau khi sửa: build 21 module, `Ran 30 tests/OK`, `link_plan_v5_minio --check` 0 thay đổi. **Đã nạp lên Web** cùng ngày (người dùng thực hiện), KB `GS9 Knowledge VNG AI` nay có 21 tài liệu. Chưa chat-test xem Agent có truy hồi trúng `doc-20` không. |
| DEC-066 | 17/08/2026 | Đang áp dụng | **Dọn hai artifact dư thừa ở root.** (1) `so-tay-tao-knowledge-base-v3.md` dời vào `audit/archive/` bằng `git mv` (giữ lịch sử). (2) `so-tay-tao-knowledge-base.html` gỡ khỏi git bằng `git rm --cached` + thêm vào `.gitignore`; **file vẫn nằm nguyên trên đĩa**, sinh lại bằng `python scripts/build_handbook.py`. Sửa kèm: nhánh fallback trong `build_handbook.py` và `recover_master_from_modules.py` trỏ sang `audit/archive/` (vẫn dò vị trí cũ ở root để bản sao repo chưa cập nhật không gãy), thay `test_master_uses_current_workspace_layout_and_release_counts` bằng `test_archived_master_is_out_of_the_build_path`, cập nhật `PROJECT.md` và `AGENTS.md`. | Người dùng đặt câu hỏi hai file này còn dùng không. **Kiểm chứng bằng số liệu, không phỏng đoán:** master chứa **20** khối `MODULE:` và **không có** `doc-20`, trong khi `docs KB/Human` có **21** file nguồn và build sinh ra **21** module gồm `doc-20` — chứng minh builder đọc Human, nhánh master là code chết vì chỉ chạy khi Human rỗng. Hệ quả trực tiếp: lúc thêm `KB-20` (DEC-065) đã phải sửa số đếm trong master và trong test chỉ để test khỏi đỏ, tức bảo trì một file không góp chữ nào vào sản phẩm. Master vẫn giữ bản lưu trữ vì ~10 tài liệu `docs KB/Dev` trích dẫn nó làm nguồn truy nguyên. Với HTML: 31,3 MB do nhúng 49 PNG base64, đã commit 3 lần, chiếm khoảng 93 MB trong `.git` 212 MB, và **mỗi lần build tạo một blob 31 MB mới vĩnh viễn**; nó là artifact sinh tự động nên không cần lịch sử. Không trùng với `gioi-thieu-knowledge-base-va-agent.html` (1,36 MB, bản giới thiệu 12 mục) — hai file khác mục đích, giữ cả hai. |
| DEC-067 | 17/08/2026 | Đang áp dụng | **Sắp xếp lại `gioi-thieu-knowledge-base-va-agent.html` thành 5 phần theo mục đích, thay 2 ảnh chat mới, sửa số liệu cũ.** Thứ tự mới: **I Vì sao cần** (01 Vấn đề) · **II KB và Agent là gì** (02, 03, 04) · **III Có lợi thế nào** (05 Thử thật — trước là mục 09) · **IV Đang có gì** (06 Kho, 07 Trợ lý, 08 Binding, 09 Trạng thái — trước là mục 12) · **V Xây tiếp thế nào** (10 Đề xuất cấu trúc, 11 Quy trình 6 bước — trước là mục 05, 12 An toàn). Thêm nhãn phần vào thanh mục lục (`li.pt`) và trước tiêu đề section mở đầu mỗi phần. Thay `case2-trace-lay-tai-lieu-anh` (1000×692) và `case3-tu-choi` (925×790) bằng ảnh mới người dùng cung cấp, cắt bỏ vùng trống và khung soạn tin. | Người dùng phản ánh nội dung "hỗn loạn". Chẩn đoán ba chỗ đứt mạch: bằng chứng giá trị (3 case thật) bị đẩy xuống sau 8 mục mô tả; bốn mục cùng nói về hiện trạng bị các mục khác chen giữa; mục Quy trình triển khai nằm giữa phần khái niệm và phần hiện trạng dù nó thuộc nhóm "xây thế nào". **Cạm bẫy gặp khi sửa:** mốc `<!-- N -->` giữa các section **không đồng nhất trong bản gốc** — `s10`, `s11` thiếu hẳn, `s12` mang số sai `<!-- 10 -->`; regex tách theo mốc chỉ bắt được 10/12 section. Phải quét theo thẻ `<section id=>` rồi tự gán lại mốc. Ghi qua tệp tạm + `os.replace` theo quy ước Drive, có sao lưu trước. Kiểm sau khi ghi: 12/12 section, 12/12 link mục lục không gãy, `<div>` cân 100/100, 16 ảnh còn nguyên, không tham chiếu ngoài, không còn `loading="lazy"`. Số liệu đã sửa: ngày cập nhật 16→17/08, "20 tài liệu"→21 (ở **hai chỗ**: bảng kho mục 06 và danh sách đã xong mục 09), và bỏ "Gắn kho cho 4 trợ lý còn thiếu" khỏi mục *đang làm tiếp* vì việc đó đã xong (DEC-060) — chuyển thành "dựng 4 kho nghiệp vụ đúng chuyên môn". **Soát lần hai còn phát hiện một mâu thuẫn nội dung:** mục 08 có ghi chú "Bốn trợ lý chưa gắn kho — câu trả lời không dựa trên nguồn nào của CFL", trong khi bảng ngay phía trên đã liệt kê cả 10 trợ lý đều có kho (4 cái ghi *Kho tạm*). Ghi chú này sót lại từ trước DEC-060. Thay bằng cạm bẫy còn nguyên giá trị và đã kiểm chứng: gắn kho xong **phải bật công cụ tra cứu**, nếu không trợ lý nhìn có kho mà thực tế không tra được (DEC-061). Kiểm tham chiếu chéo sau khi đảo thứ tự: cả 3 chỗ trong bài trỏ "mục 10" đều vẫn đúng vì mục Đề xuất cấu trúc giữ nguyên số 10. **Còn tồn tại (cố ý không sửa):** `id` của section không còn khớp số hiển thị (ví dụ `id="s5"` nay hiện là mục 11) — vô hình với người xem, chỉ gây rối cho người sửa file sau này. |
| DEC-068 | 17/08/2026 | Đang áp dụng | **Chuyển ảnh minh hoạ "ảnh hiện được ngay trong câu trả lời" từ mục 12 Nguyên tắc an toàn sang mục 02 Knowledge Base là gì.** Ảnh (`alt="Câu trả lời hiển thị được cả ảnh minh họa lấy từ kho"`) nằm lạc trong mục an toàn — nội dung ảnh là một câu trả lời chat có nhúng ảnh minh hoạ, không liên quan gì tới guardrail. Chuyển tới ngay sau bảng "Kho xử lý được những gì" ở mục 02, minh hoạ đúng dòng "Ảnh PNG, JPG — ảnh hiện được ngay trong câu trả lời". | Phát hiện khi rà soát toàn văn bản theo yêu cầu người dùng "tinh chỉnh lại file". **Xác nhận đây là lỗi có từ trước, không phải do đợt sắp xếp lại DEC-067 gây ra** — đối chiếu bản backup trước khi sắp xếp lại vẫn thấy ảnh ở đúng vị trí sai này. Việc di chuyển base64 (46.116 ký tự) không dùng Edit trực tiếp được vì chuỗi quá dài; viết script Python cắt-dán theo mốc `<figure>` rồi kiểm lại bằng đếm alt text (phải ra đúng 2 — 1 trong `img alt`, 1 trong `figcaption`, tức chỉ 1 khối `<figure>` duy nhất). Không tìm thêm lỗi tương tự nào khác sau khi rà hết 12 mục bằng văn bản trích xuất từ DOM. |
| DEC-069 | 17/08/2026 | Đang áp dụng | **Đính chính DEC-052: đồng bộ Google Drive KHÔNG mặc định làm chết URI ảnh — lần chết 49/49 URI ngày 11/08 có nguyên nhân riêng là di trú KB, không phải bản chất của "đồng bộ".** Bằng chứng mới: người dùng crop lại 23 ảnh cũ (`image-01`→`image-14`, `image-26`→`image-34`, trừ vài số) và thêm 3 ảnh mới (`image-50`→`image-52`), đồng bộ Drive → KB. Đọc qua MCP `list_documents`: đúng **26/52 ảnh** (23 sửa + 3 mới) chuyển sang `pending`/`processing`/`finalizing` để xử lý lại; **26/52 ảnh còn lại — những ảnh người dùng KHÔNG đụng tới — vẫn giữ nguyên `parse_status: completed` VÀ giữ nguyên URI cũ**, đối chiếu từng ký tự khớp `image-map.json` đang có (`image-15`, `image-49` kiểm mẫu, khớp tuyệt đối). | Sự kiện DEC-052 gốc (URI 49/49 chết ngày 11/08→16/08) **không phải một lượt đồng bộ Drive thông thường** — đó là hệ quả của DEC-043 (di trú 49 ảnh từ KB `GS9 Knowledge VNG - Image Assets` sang KB `GS9 Knowledge VNG AI`, tức tạo lại thành **tài liệu mới trong một KB khác**, chắc chắn sinh document ID và URI mới cho toàn bộ). Bản chất thật của đồng bộ Drive tăng dần trong **cùng một KB** đã được chứng minh ở đây: **chỉ file có nội dung đổi (hash khác) mới được xử lý lại và nhận URI mới; file không đổi giữ nguyên document ID và URI**, kể cả khi cùng một lượt sync chạm vào cả thư mục. Đây là bằng chứng thứ hai củng cố kết luận tương tự từng thấy ở `audit/google-drive-incremental-sync-test-2026-08-15-baseline.md` (vòng 2, KB `Test`) nhưng lần này đo trên KB thật `GS9 Knowledge VNG AI` với 52 tài liệu, qua MCP thay vì chỉ quan sát UI. **Sửa lại quy tắc vận hành:** không cần rà toàn bộ `image-map.json` sau mọi lần đồng bộ — chỉ cần lấy URI mới cho đúng tập file đã đổi, xác định qua `parse_status` chuyển khỏi `completed`. Đã sửa chú thích trong DEC-052. |
| DEC-070 | 17/08/2026 | Đang áp dụng | **Thay 7 ảnh trong `gioi-thieu-knowledge-base-va-agent.html` bằng bản đã crop lại của người dùng** — `image-01`, `image-02`, `image-08`, `image-26`, `image-27`, `image-29`, `image-33`. Nội dung không đổi (người dùng xác nhận), chỉ khung hình gọn hơn (bỏ viền trình duyệt/khoảng trắng thừa). | File thuyết trình không có script build hay manifest nối ảnh nhúng với file nguồn trong `knowledge/` — ảnh được ghép tay từ phiên trước, alt text paraphrase khác chữ trong `docs KB/Human`. Phải xác định từng cặp bằng cách trích ảnh đang nhúng ra rồi **so khớp bằng mắt** với ảnh nguồn (đọc cả hai, đối chiếu nội dung hiển thị) trước khi thay — không suy đoán theo tên biến số. Xác nhận đúng 7/7 cặp trước khi sửa, trong đó `image-33` (nội dung "Nguồn tham khảo") là cặp khó nhất vì alt text không gợi ý rõ chủ đề, xác nhận bằng cách đọc trùng khớp nguyên văn câu hỏi hiển thị trong ảnh. Resize về chiều rộng 1100px (khớp quy ước ảnh khác trong bài), JPEG quality 82. Không đổi 2 ảnh còn lại nghi ngờ (`image-05`→15, `image-16`→17) vì không nằm trong danh sách người dùng đã sửa. |
| DEC-064 | 17/08/2026 | Đang áp dụng | **Sự cố Drive lặp lại dạng nhẹ hơn DEC-059, không mất dữ liệu.** Đầu phiên 5, `git status` cho thấy toàn bộ `knowledge/` (140 file) bị đánh dấu Deleted và có thư mục untracked `knowledge (1)/` — đúng pattern DEC-059. Người dùng tự đổi tên `knowledge (1)` → `knowledge` (đúng runbook, giữ Drive ID). Sau khi Drive sync xong: `git status --short` rỗng tuyệt đối, `git diff --stat origin/main HEAD` rỗng, HEAD khớp `origin/main` (`3af8a30`). Chỉ còn sót `scripts/__pycache__ (1)` và `tests/__pycache__ (1)` (bytecode đã gitignore, đã xoá). | Runbook DEC-059 mục 6 HANDOFF **hoạt động đúng lần 2** — xác nhận đây là cách xử lý chuẩn, không phải may mắn lần đầu. Điểm khác lần này: không sửa/xoá gì trước khi Drive sync xong và đối chiếu `git diff --stat origin/main HEAD` rỗng; không cần clone bản sạch từ GitHub vì việc đổi tên đã đúng ngay từ đầu. Bài học thêm: sau khi rename xong vẫn phải đợi Drive sync **hoàn toàn** rồi mới chạy `git status` — chạy quá sớm dễ thấy trạng thái giả (lệnh `git status --short` từng timeout 2 phút lúc Drive còn đang đồng bộ). |
| DEC-071 | 18/08/2026 | Đang áp dụng | **Hàng đợi xử lý tài liệu của nền tảng tắc thật — không phải lỗi file, không phải lỗi cấu hình KB.** 26 ảnh nạp lúc 17/08 18:48–18:49 tới 18/08 04:20 vẫn 0 `completed` (21 `pending`, 4 `processing`, 1 `finalizing`). Bằng chứng quyết định lấy từ panel **"Xem tiến trình"** (menu ⋯ của tài liệu trên Web, đọc qua shadow DOM): `image-28` và `image-02` đều ở trạng thái **`Chờ` · 0/5 giai đoạn**, cả 5 bước (Phân tích tài liệu, Chia đoạn, Vector hóa, Đa phương thức, Hậu xử lý) đều "Đang chờ", kèm bộ đếm lần thử `#1 #2` và `#1 #2 #3` — job được xếp hàng, hết giờ, xếp lại, không worker nào chạy. `image-28` còn có `failed_stages.summary = "failed to update knowledge: context deadline exceeded"` và từng tụt từ `finalizing` về `pending`. Không tài liệu nào rơi vào trạng thái "Lỗi". | **Hệ quả vận hành:** "Phân tích lại" chỉ thêm một lần thử vào hàng đợi đang không chạy — **không bấm hàng loạt**, không dùng nó như cách chữa. Việc đúng là báo team vận hành nền tảng kèm KB id `cefadf09-4187-46ac-a765-591e3255a4a4`, tenant `10012`, 47 tài liệu treo (26 png + 21 md), mốc treo và thông điệp lỗi. Gate giữ nguyên 1 FAIL (`test_project_image_map_covers_all_merged_assets`) cho tới khi ảnh xong — không che bằng `--allow-missing-minio`. **Chưa chứng minh:** vì sao worker không chạy (không có quyền xem log nền tảng). |
| DEC-072 | 18/08/2026 | Đang áp dụng | **Xác nhận DEC-069 lần hai, lần này với chế độ đồng bộ `Toàn bộ`.** Cấu hình nguồn dữ liệu đọc trực tiếp trên Web (Cấu hình Knowledge Base → Nguồn dữ liệu): nguồn `GS9 Knowledge Base VNG AI` (Google Drive), **Chế độ đồng bộ = Toàn bộ**, phạm vi "Mọi tệp", lịch hiển thị "Vào 02:00", đồng bộ gần nhất ~00:00 ngày 18/08, kết quả **Thành công**. Lần chạy đó **tạo lại toàn bộ 21 `doc-*.md`** (document ID mới, `pending`, `disabled`) nhưng **26 ảnh không đổi nội dung vẫn giữ nguyên document cũ và URI cũ** — đối chiếu bằng script: **26/26 URI khớp tuyệt đối với `image-map.json` sinh ngày 16/08, 0 lệch**. | **Quy tắc dùng từ nay:** kể cả chế độ `Toàn bộ`, chỉ file đổi hash hoặc file mới mới bị tạo lại document/URI; không cần rà lại toàn bộ `image-map.json` sau mỗi lần sync — chỉ lấy URI cho đúng tập file vừa đổi trạng thái. **Hệ quả cần nhớ:** mỗi lần sửa hàng loạt `.md` rồi đồng bộ là cả kho chữ rơi về `pending`/`disabled` một khoảng thời gian, Agent tra kho này sẽ hụt nguồn — nên tránh sửa hàng loạt sát giờ cần demo. **Chưa khớp, cần làm rõ khi có dịp:** UI ghi lịch "Vào 02:00", người dùng nhớ là 12 tiếng, còn lần chạy gần nhất lại rơi vào ~00:00. |
